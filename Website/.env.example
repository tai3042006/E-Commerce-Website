# Copy to .env and adjust as needed.

# API base URL — used by the React frontend when making fetch calls.
# The Vite dev server proxies /api → http://localhost:4000 automatically,
# so you only need this for production builds pointing at a remote server.
# VITE_API_URL=http://localhost:4000/api
